<%= form_with model: @announcement do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :content %>
  <%= form.rich_text_area :content %>

  <%= form.submit %>
<% end %>
