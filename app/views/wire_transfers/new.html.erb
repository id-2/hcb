<% title "Send a wire transfer" %>
<% page_sm %>
<%= render "events/nav", selected: :transfers %>

<h1>Send a wire transfer</h1>

<%= render "callout", type: "warning", title: "Important info about wire transfers", footer: :questions do %>
  <ul>
    <li><strong>Sending via wire is irreversible</strong>—you cannot cancel.</li>
    <li>If you enter the wrong destination here, the money will permanently leave your account regardless.</li>
    <li>These are international banking system restrictions, so we have limited ability to fix mistakes.</li>
  </ul>
<% end %>

<% unless policy(@event).create_transfer? %>
  <%= render "events/transfers/unauthorized_callout" %>
<% end %>

<%= render "form" %>
