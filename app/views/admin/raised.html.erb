<% title "MONEY RAISED" %>

<h1>
  <%= render_money @raised %>
  raised.
</h1>

<%= paginate @events %>

<%= form_with local: true, url: events_admin_index_path, method: :get do |form| %>
  <%= form.text_field :q, value: params[:q], style: "vertical-align: top;", placeholder: "Search" %>
  <div class="inline-block">
    <%= render partial: "events_filter", locals: { form: } %>
  </div>
<% end %>

<%= page_entries_info @events, entry_name: "organizations" %>

<table>
  <thead>
    <tr>
      <th class="w-16">ID</th>
      <th class="w-40">Date</th>
      <th class="w-80">Organization</th>
      <th>Raised</th>
      <th>Balance</th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
      <tr class="<%= "admin-bg-pending" if event.pending? || event.unapproved? || event.demo_mode? %> <%= "admin-bg-orange" if event.omit_stats? %>">
        <td><%= event.id %></td>
        <% if event.activated_at? %>
          <td style="display: flex; align-items: center; gap: 5px" title="Demo org created on <%= event.created_at.strftime("%Y-%m-%d") %> (<%= distance_of_time_in_words(event.created_at, event.activated_at) %> earlier)">
            <%= event.activated_at.strftime("%Y-%m-%d") %> <%= inline_icon "info", size: 24, style: "color: #338eda" %>
          </td>
        <% else %>
          <td><%= event.created_at.strftime("%Y-%m-%d") %></td>
        <% end %>
        <td>
          <%= link_to event_path(event) do %>
            <%= event.name.upcase %>
          <% end %>
        </td>
        <td>
          <%= async_frame_to event_raised_admin_path(event) do %>
            <em>$ –</em>
          <% end %>
        </td>
        <td>
          <%= async_frame_to event_balance_admin_path(event) do %>
            <em>$ –</em>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @events %>
