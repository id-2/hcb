<div data-controller="menu" class="pt2">
  <%= link_to @event, class: "#{"pointer-events-none" if !signed_in?} text-decoration-none text-3xl gap-4 text-inherit event-title -m-2 my-2 px-2 rounded-lg transition-colors flex items-center tracking-tight", style: "color: var(--primary)!important; font-weight: 800", data: ({ action: "click->menu#toggle click@document->menu#close keydown@document->menu#keydown", menu_target: "toggle" } if show_org_switcher?) do %>
    <%= @event.name %>
    <%= inline_icon "down-caret", class: "event-title-dropdown-icon" if show_org_switcher? %>
  <% end %>
  <% if show_org_switcher? %>
    <div class="menu__content menu__content--switcher sm:w-[275px]" data-menu-target="content">
      <p class="muted font-black text-sm mb-0 uppercase m-1">Organziations</p>
      <% current_user.events.not_hidden.with_attached_logo.map do |event| %>i
        <%= link_to event, title: event.name, class: "gap-2" do %>
          <% if event.logo.attached? %>
            <%= image_tag event.logo, height: 24, width: 24, class: "rounded" %>
          <% else %>
            <div style="height: 24px; width: 24px;" class="bg-neutral-500/30 rounded shrink-none"></div>
          <% end %>
          <span class="truncate"><%= event.name %></span>
        <% end %>
      <% end %>

      <a data-behavior="toggle_theme" class="gap-2">
        <%= inline_icon "moon", size: 24 %>
        <%= inline_icon "moon-fill", size: 24, style: "display: none;" %>
        <span>Toggle theme</span>
      </a>
    </div>
  <% end %>
</div>
