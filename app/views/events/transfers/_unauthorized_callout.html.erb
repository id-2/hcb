<%= render "callout", type: "info", title: @event.demo_mode? ? "You can't transfer money" : "Transfers are restricted for members" do %>
  <% if @event.demo_mode? %>
    <p class="m0">This is because this organization is in Playground Mode.</p>
  <% else %>
    <ul class="m0">
      <li>Because you're not a manager, your transfer privileges are limited.</li>
      <li>If you need to send money to another HCB organization, you can request a transfer within HCB (subject to manager authorization)
      <li>If you need to send another type of transfer, ask your manager to change your role.</li>
      <li>To learn more, visit
        the <%= link_to "user roles explainer page", roles_path, data: { turbo_frame: "_top" } %>.
      </li>
    </ul>
  <% end %>
<% end %>
