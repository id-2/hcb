<%= turbo_frame_tag :my_cards_shipping do %>
  <% if @stripe_cards.any? %>
    <div class="grid grid--split mb3">
      <% @stripe_cards.each do |card| %>
        <%= render "stripe_cards/shipping", stripe_card: card, event: @event %>
      <% end %>
    </div>
  <% end %>
<% end %>
